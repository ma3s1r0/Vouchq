/**
 * <b>registry</b> — the trust core: approve &amp; pin (박제) a definition's
 * SHA-256 snapshot as the canonical version, then detect drift by re-hashing
 * and comparing. See Linear MA3-77, MA3-78.
 */
package com.vouchq.registry;
