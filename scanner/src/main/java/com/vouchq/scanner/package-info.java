/**
 * <b>scanner</b> — pure-Java library (AGPL-3.0, like the whole repo). Rule-based risk detection over parsed
 * Skill (later MCP) definitions: prompt injection, secret exposure, data
 * exfiltration, and dangerous commands. Produces a scored
 * {@link com.vouchq.scanner.ScanResult}. The rule set is extensible. No
 * framework deps. See Linear MA3-74.
 */
package com.vouchq.scanner;
